name: scan

on:
  push:
    branches:
      - "dev"

jobs:
  container_scan:
    runs-on: ubuntu-latest
    steps:
      - name: Hello from dev!
        run: echo "Hello, container_scan!"
        env:
          SUPER_SECRET: ${{ secrets.SuperSecret }}