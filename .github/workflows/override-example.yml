name: scan

on:
  push:
    branches:
      - "test"

jobs:
  container_scan1:
    runs-on: ubuntu-latest
    steps:
      - name: Hello from test!
        run: echo "Hello22 container_scan2!"
        env:
          SUPER_SECRET: ${{ secrets.SuperSecret }}
