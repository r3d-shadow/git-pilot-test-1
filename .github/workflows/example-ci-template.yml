name: scan

on:
  push:
    branches:
      - "test"

jobs:
  container_scan1:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Hello World
        run: echo "Hello, World from job CONTAINER_SCAN1!"